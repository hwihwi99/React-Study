console.log("chapter 05");

// 1. Number Type
let num1 = 27;
let num2 = 1.5;
let num3 = -2;

let inf = Infinity;
let mInf = -Infinity;

let nan = NaN;

//2. String Type
let name = "이름";
let address = "주소";
let introduce = name + address; // 이름주소 로 문자열 concat 연산 가능

let introduceText = `${name}dms ${address}에 거주합니다`; // 변수를 넣어서 바로 문자열로 만들기

// 3. Boolean Type : true / false

// 4. Null Typr (아무것도 없다)

// 5. Undefined Type
